# 奥山貴希 ポートフォリオ

WebアプリとUnity作品を、短い説明で一覧できる1ページのポートフォリオサイトです。

Codex担当の役割、触る範囲、統括への引渡し条件は [`AGENTS.md`](AGENTS.md) を参照してください。

- 公開ページ: <https://okuyamatakaki.github.io/Portfolios/>
- 掲載内容: Spring Boot日記アプリ、Unityゲーム・試作
- 入口: `index.html`
- デザイン: `assets/css/style.css`
- 日英切替: `assets/js/localization.js`

## 再開するとき

このリポジトリ直下が編集用の正本です。iCloudのコピーや公開ページを直接編集元にしません。

1. このREADMEと `Docs/THIRD_PARTY_NOTICES.md` を読み、公開前の未解決事項を確認する。
2. 作品内容は `index.html`、日英文言は `assets/js/localization.js`、見た目は `assets/css/style.css` を編集する。
3. `node scripts/validate-site.js` を実行する。
4. ローカルサーバーで日本語・Englishと共通6画面幅を確認する。
5. `git diff` で意図した変更だけか確認してからコミットする。

公開ページはGitHubの `main` ブランチをpushしたときに更新されます。ローカルコミットだけでは公開ページは変わりません。

公開前の復元基準は`522fd932c37f0bb25e7d9ddc4be0fb5b63159e08`、実行ファイル候補の基準は`d5885487d962373965dae69853b58a90cbc18633`です。現在の公開HEADは`git log -1 --oneline`、品質と復元条件は`Docs/RELEASE_DECISION_JA.md`で確認できます。復元時も`reset`や`clean`で作業を一括破棄せず、対象ファイルを限定してください。

## 主な更新場所

| 目的 | ファイル |
| --- | --- |
| 作品名・説明・リンク・SEO | `index.html` |
| 色・文字・余白・レスポンシブ表示 | `assets/css/style.css` |
| 日本語・Englishの表示文言とfallback | `assets/js/localization.js` |
| 作品画像・OGP・ファビコン | `assets/images/` |
| ローカル表示フォント | `assets/fonts/PixelMplus12-Regular.ttf` |
| ローカライズ静的検査 | `scripts/validate-localization.js` |
| サイト全体の静的検査 | `scripts/validate-site.js` |
| 素材・ライセンス台帳 | `Docs/THIRD_PARTY_NOTICES.md` |
| 最新の検証結果と残件 | `Docs/VALIDATION_20260901.md` |

作品数を変更した場合は、画面上部の `06` と作品数表示も合わせて更新します。外部リンクを新しいタブで開く場合は、`target="_blank"` と `rel="noopener noreferrer"` をセットで指定します。

## 共通化の方針

共通化するのは、同じ意味と変更理由を持つ範囲だけです。

- 色・書体・共通幅はCSS変数に集約し、8pxを基準にした余白、0.8rem以上の補助文字、44px以上の操作領域を画面全体で共有します。
- ボタン、カード、見出し、フォーカス表示、レスポンシブ規則は共通クラスで管理します。
- CSSは「構造と操作性」「INDUSTRIAL SKIN（作品固有の表現）」「レスポンシブ」の順に分け、レイアウト変更と外観変更を切り分けます。
- 言語の選択・保存・fallbackは共通処理にし、作品固有の名称・説明・リンクはこのサイトの翻訳表とHTMLに残します。
- 他プロジェクトの作品データ、画像、公開設定、ゲームコードは共有フォルダー化しません。再利用する場合も、検証済みの考え方や小さな仕組みに限定します。

表示の流れは `index.html` の日本語初期表示 → `localization.js` の言語選択と文言差し替え → `style.css` の表示調整です。外部ライブラリやビルド処理はありません。表示フォントはM+ FONT LICENSEのPixelMplusをリポジトリ内から読み込み、Google Fontsへの通信は行いません。

## 日本語 / English

- ヘッダーの切替は最初の画面から操作でき、各ボタンは44px以上のタップ領域を持ちます。
- 選択言語はlocalStorageの `portfolio.language` に保存します。
- 保存がなければブラウザの日本語・英語設定を使い、それ以外の言語環境ではEnglishを選びます。言語情報を取得できない場合と欠落キーのfallbackは日本語です。
- 表示文言、メタデータ、画像alt、aria-labelは `localization.js` の言語表で管理します。作品名と技術名は固有名として必要な範囲で維持します。
- 日記アプリの機能一覧は「主な機能を見る / View key features」で開き、最初の画面へ説明を詰め込みすぎない構成です。
- 紹介動画は権利者確認が終わるまで無効です。動画IDやiframe生成処理を公開ページへ置かず、YouTubeへの通信も行いません。
- 実行時の欠落・未使用キーはコンソールと `window.__portfolioI18nDiagnostics` で確認できます。

## 画像の状態

- `og-portfolio-2026.png`: 現在使用するSNS共有画像です。編集可能な第一者SVG原本と制作記録があります。
- `factory-break-2026.jpg`: ローカルの名称変更後候補を実行して取得した、30秒！ファクトリーブレイクの現在の作品画像です。
- `roulette-portfolio-2026.svg`: 外部入力を使わずローカルで新規制作した、編集可能な現在のルーレット紹介画像です。
- `favicon.svg`: 外部入力を使わずローカルで新規制作したブラウザタブ用の幾何学ロゴです。
- `roulette.png`、`og-industrial.png`、`og.png`: 来歴を確定できない旧画像です。公開参照を外し、ごみ箱へ移動済みです。
- `block-breaker.png`、`shift-breaker-2026.png`: 現在の名称・掲載内容と一致しない旧画像です。`factory-break-2026.jpg`へ置き換え、ごみ箱へ移動済みです。
- `factory-break-2026.png`: 読込み途中だけが写った未追跡の失敗画像だったため、ごみ箱へ移動済みです。

旧画像を復元する必要がある場合は、まずごみ箱を確認します。Git追跡されていた画像は削除前のローカル履歴にも残りますが、権利・内容の問題を解消せず公開参照へ戻しません。

同一内容の重複画像はありません。

## 著作権・素材の確認

素材、フォント、外部サービス、AI生成物の来歴と未解決事項は、[素材・ライセンス・来歴台帳](Docs/THIRD_PARTY_NOTICES.md)で管理します。次回の公開更新前に、台帳の「結論」を確認してください。

PixelMplusの許諾文は `Docs/Licenses/PixelMplus` に保存しています。通常の画面へ出典一覧は表示せず、開発者がこのREADMEから確認できる構成です。

## ローカル確認

静的検査はNode.js 24.19.0、ローカル配信はPython 3.12.13で確認しています。追加パッケージは不要です。

プロジェクト直下で次を実行し、<http://127.0.0.1:8000/> を開きます。

```powershell
python -m http.server 8000 --bind 127.0.0.1
```

ブラウザを使わない静的検査:

```powershell
node --check assets/js/localization.js
node scripts/validate-site.js
```

`validate-site.js` は、ローカライズ検査に加えて、参照ファイル、重複ID、ページ内リンク、外部リンクの安全属性、画像・iframeの代替情報、作品数、CSS構文の基本条件を確認します。

公開前に確認する項目:

1. 360×640、390×844、844×390、768×1024、1280×720、1920×1080で日本語とEnglishを切り替え、文字切れや横スクロールがない。200%文字拡大も別に確認する。
2. 言語選択を再読み込み後も保持し、保存なしの日本語・英語環境で初期言語が正しい。
3. 画像、ページ内リンク、GitHub、Render、unityroomが開き、動画欄は無効状態でYouTubeへ通信しない。
4. Tabキーで言語切替・ナビゲーション・主要リンク・詳細開閉を操作できる。
5. OGP画像URLと掲載作品数が最新になっている。
6. 静的検査、`git status --short`、`git diff --check` が成功する。

## 共通の公開品質ゲート

このサイトは技術的なローカル候補であり、共通基準を満たす公開完了版ではありません。自動検査や画面幅シミュレーションを、実機や人の承認の代わりにしません。

| 項目 | 現在の状態 |
| --- | --- |
| 日英・privacy・権利台帳・秘密情報 | ローカル候補で確認済み |
| レスポンシブ | 共通6画面幅の日英と、640×360・422×195の200%相当領域をChromiumで確認済み。実Chrome／Safariの200%表示と実機は未確認 |
| 問い合わせ | GitHubプロフィールを仮窓口とすることは利用者決定済み。正式な非公開窓口と公開版の最終採用は未確認 |
| 人間中心の表示 | 0.8rem以上の補助文字、44px以上の操作対象、フォーカス表示を確認。独立した人の快適性確認は未確認 |
| 30 FPS相当 | Chrome 152 Headlessで約58.0 rAF/s。静的サイトのローカル事前指標で、物理実機は未確認 |
| 20秒以内に操作可能 | 同一PCのローカル配信はload約0.30秒。公開回線・実機は未確認。リンク先の日記アプリの起動待ちは別製品として扱う |
| 再読込・クラッシュ復帰 | 言語選択の再読込保持のみ確認。クラッシュ後復帰は未確認 |
| 80台実機 | 0 / 80台。Androidスマートフォン、iPhone、タブレット、Windows PCを各20台確認する |

端末エミュレーションは事前確認であり、80台実機へ数えません。端末購入や有料端末ファームは、人の明示承認なしに利用しません。公開可否の正式な状態と直前確認は [`Docs/RELEASE_DECISION_JA.md`](Docs/RELEASE_DECISION_JA.md) に集約します。

## 公開状態と復旧

- 公開前に [`Docs/RELEASE_DECISION_JA.md`](Docs/RELEASE_DECISION_JA.md) へ、確認した権利、公開差分、費用、復元方法、人の判断を記録します。
- この版には、日英切替、翻訳表、静的検査、素材台帳、Git設定が含まれます。
- GitHubへpushする前に、素材台帳の未解決事項と実ブラウザの最終目視を完了します。
- 変更を戻すときは、最初に `git diff` と `git status --short` で対象を確認します。作業中の変更を一括で破棄せず、必要なファイルだけを直前コミットから復元します。
- iCloudバックアップから復元する場合は、別のローカルフォルダーへコピーして検査してから正本と入れ替えます。

## セキュリティとバックアップ

- パスワード、APIキー、個人情報、`.env` はリポジトリへ追加しません。
- 読み込める外部リソースはContent Security Policyで必要な送信先に限定します。
- 編集・表示確認・Git操作はローカル作業フォルダーだけで行います。
- iCloud Driveは全作業完了後のバックアップ出力専用です。iCloud内では編集・ビルド・テストを行いません。
- バックアップ時は `.git/` を含めず、復元時はローカルの別フォルダーへ展開してから開きます。

## 最終確認記録

2026-08-30に、ローカル素材の欠落なし、主要外部リンクの到達、1280px・768px・375pxでの表示、秘密情報・フォーム送信処理がないことを確認しました。

2026-08-31に、日英カタログの欠落・未使用キー、初期言語選択、fallback、localStorage処理、HTML/CSS/JavaScript構文、参照ファイルを静的検査しました。

2026-09-01にGoogle Fonts参照を除去し、M+ FONT LICENSEのPixelMplusをローカル配信へ変更しました。静的検査は63キー、6作品、参照欠落0、重複ID 0で合格しています。実ブラウザで1280px日本語、768px英語、375px日本語／英語を確認し、英語選択が再読み込み後も保持されること、フォントがHTTP 200で読み込まれること、ブラウザ警告・エラー0件を確認しました。文字切れ、横スクロール、主要ボタンの重なりはありません。確認用サーバーとタブは終了済みです。

同日にプライバシー画面とフッター導線を追加し、YouTube紹介動画は利用者が確認ボタンを押すまでiframeを生成しない方式へ変更しました。最新版の静的検査は66キー、6作品、参照欠落0、重複ID 0で合格しています。実ブラウザで1280×720と390×844を確認し、横はみ出し0、動画読込ボタン50px、初期iframe 0件、日英切替、プライバシー画面、外部リンクの安全属性、ブラウザ警告・エラー0件を確認しました。外部公開と動画読込ボタンの押下は行っていません。

その後の権利監査で、来歴不明のOG画像を編集可能な第一者SVG原本付き画像へ差し替えました。動画は権利者確認まで動画ID・iframe生成処理・外部通信をすべて無効にし、30秒！ファクトリーブレイク欄は名称変更後の検証済み候補から取得した作品画面へ差し替えました。制作記録は `Docs/Provenance/`、権利判断は `Docs/THIRD_PARTY_NOTICES.md` に保存しています。

差し替え後に、1280×720の日本語表示と390×844のEnglish表示を実ブラウザで再確認しました。横あふれ0、欠損画像0、iframe 0、動画ID 0、動画ボタン無効、すべての有効なリンク・ボタン・詳細操作が高さ44px以上、ローカル配信ファイルはHTTP 200または304でした。プライバシー画面の日英記載も確認し、確認用サーバーとタブは終了しました。iPhone・Android実機確認、Git反映、外部公開は未実施です。

同日の公開候補監査では、390×844のトップとプライバシー画面を再確認し、本文内のGitHubリンクを含む全操作対象を44px以上へ統一しました。両画面とも横あふれ0、iframe 0、ブラウザ警告・エラー0件です。その後、権利記録が不足していたルーレット画像とfaviconを、制作記録付きの第一者SVGへ差し替えました。

続行監査では、言語切替、作品数、カテゴリなどの補助文字を共通変数 `--font-size-small` の0.8remへ統一しました。390×844の日本語・Englishとプライバシー画面で、横あふれ0、44px未満の操作対象0、iframe 0、警告・エラー0件を再確認しています。言語選択の再読み込み後の保持、日記アプリの詳細開閉、キーボードフォーカスの可視表示も確認済みです。

旧画像3点がごみ箱へ移動された後の再監査では、作業フォルダーに3点が存在しないこと、コミット開始時にGitで削除として識別されたこと、現行HTML・CSS・JavaScriptから参照されていないことを確認しました。現在の公開ページは `origin/main` を基準としているため、ローカルコミットとの差分はまだ公開されていません。

2026-09-02に新共通ルールを反映し、80台実機、30 FPS相当、20秒以内の操作開始、クラッシュ後復帰、必須画面幅、200%文字、人の快適性確認を公開品質ゲートへ追加しました。既存証拠で確認できない項目は未完了のままです。公開候補の実行ファイル9点には構造ハッシュを付け、公開判断記録に復帰点と人の未確認欄を残しました。

2026-09-03にCodex In-app BrowserのChromium実ブラウザーで、未確認だった`1920×1080`、`844×390`、`360×640`を日本語／Englishで追加確認しました。既存の`1280×720`、`390×844`、`768×1024`と合わせ、共通6画面幅の横あふれ0、重複ID 0、44px未満の表示中操作対象0、ブラウザーwarning／error 0です。`640×360`と`422×195`は200%相当の半分CSS領域として再配置を確認し、横あふれ0でした。422×195では縦スクロールが必要ですが、先頭のロゴ、言語、ナビゲーションは切れずに操作できます。遅延読込み画像2点は初期画面外では未読込みでしたが、スクロール後に両方ともHTTP 200かつ自然寸法ありで表示されました。プライバシー画面も同じ5領域で横あふれ0、44px未満0、日英本文を確認しています。確認後は言語を日本語へ戻し、viewport上書き、タブ、ローカルサーバーを終了しました。実Chrome／Safariの200%表示、物理端末80台、性能、クラッシュ復帰、人による快適性は未確認です。

同日、Haita欄を確定名称`注ぐ！止める！タイミングチャレンジ / POUR & STOP: TIMING CHALLENGE`と、現実の製品操作に見えにくいタイミングゲームの短文へ更新しました。Chrome 152 Headlessで共通6画面幅と200%相当領域を日英再確認し、横あふれ0、44px未満0、意図しない文字切れ0、warning／error・HTTPエラー・意図しない外部通信0でした。390×844の長い日英名称もカード内で安全に折り返すことを補助目視しました。ローカルloadは約0.30秒、rAFは約58.0/sです。表示中Chrome、Safari、物理端末80台、人の快適性は未確認です。

既存GitHub Pagesも同日に読み取り確認しました。HTTP 200で390×844の横あふれはありませんが、日英切替、プライバシー導線、新しいHaita名はまだ反映されず、旧名`ハイターチャレンジ`のままです。ローカル修正と公開版を混同せず、pushは公開差分と復元方法の直前確認後だけ行います。
