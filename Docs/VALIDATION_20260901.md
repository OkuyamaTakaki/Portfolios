# 検証記録 2026-09-01〜2026-09-03

## 結論

現在のローカル候補は、日英表示、共通6画面幅、200%相当の半分CSS領域、操作領域、プライバシー導線、CSP、秘密情報、素材来歴の軽量検査に合格しています。Haitaの確定名称・安全な短文と、GitHubプロフィールを仮問い合わせ先とする利用者決定も反映済みです。Chrome 152 Headlessでは約58 rAF/sとローカルload約0.30秒を確認しました。80台実機、実クラッシュ後復帰、表示中Chrome／Safariの200%表示、人の快適性確認は未完了です。push、外部公開、iCloudバックアップは行っていません。

正本は、この`Docs`フォルダーの親にある`OkuyamaTakakiPortfolios`リポジトリです。ブランチは`main`、ローカルHEADは`a3e2307`、公開基準は`origin/main`の`522fd93`です。ローカルは3commit先で、今回のHaita名称・説明は未commitです。

公開可否の正式な区分と人の未確認欄は、[公開の人による判断記録](RELEASE_DECISION_JA.md)を参照してください。

## 2026-09-02 低負荷再照合

開始時の復帰点:

- 正本: `OkuyamaTakakiPortfolios`。保存プロジェクトが指す1階層上の他内容へは書き込まない
- `main`、ローカルHEAD `6cba0d5a1780fd36cc6ca1f7e6fbfe83ec5a09e7`
- 公開基準 `522fd932c37f0bb25e7d9ddc4be0fb5b63159e08`
- 既存未コミット25パス: 変更7、削除3、新規15、ステージ済み0
- Gitロックなし、別ライターなし、ローカルサーバーなし
- 復旧方針: `reset`、`clean`、一括復元を使わず、必要な場合は別の場所で基準と比較する

実行した軽量検査:

- JavaScript構文、ローカライズ、HTML/CSS/JS参照、安全属性: 合格
- `git diff --check`: 合格。第三者原文 `Docs/Licenses/PixelMplus/README.txt` 44行目の署名区切りだけは、配布元の末尾空白を改変しないよう `.gitattributes` で対象限定の例外を明示
- READMEとDocs内のローカルMarkdownリンク5件: 欠落0
- 秘密情報パターン: 0件
- iframe、YouTube、Google Fonts、公開対象外画像の実行時参照: 0件
- 新しいルーレットSVG、favicon、OG画像、SHIFT BREAKER画像、PixelMplusのハッシュ: 台帳と一致
- 実行ファイル9点の構造ハッシュ: SHA-256 `379FAA63D6575E18F41227E578278351E219903683FF91B3C5A47675D2091E82`。`RELEASE_DECISION_JA.md`に列挙した順序、タブ区切り、大文字SHA-256、LF、末尾改行なしで再現

2026-09-02はPC負荷を抑えるためブラウザーの大量検査をせず、下記の2026-09-01証拠を再利用しました。新しいルーレットSVGとfaviconは静的参照・ハッシュ・制作記録を照合しましたが、80台実機や現在候補の全ブラウザー回帰を実施した意味にはしません。

## 2026-09-03 画面幅と仮問い合わせ先の追加確認

- Codex In-app BrowserのChromiumで、360×640、390×844、844×390、768×1024、1280×720、1920×1080の共通6画面幅を日本語／Englishで確認
- 共通6画面幅は横あふれ0、重複ID 0、表示中の44px未満操作対象0、ブラウザーwarning／error 0
- 640×360と422×195を200%相当の半分CSS領域として確認し、横あふれ0。422×195は縦スクロールが必要だが、先頭のロゴ、言語、ナビゲーションは切れずに操作可能
- 遅延読込み画像2点はスクロール後にHTTP 200、自然寸法あり。プライバシー画面も確認対象領域で横あふれ0、44px未満0、日英本文を確認
- 画面領域の切替は80台実機へ数えない。実Chrome／Safariの200%表示、物理端末、性能、人の快適性は別証拠として未確認
- `index.html` と `privacy.html` のGitHubプロフィール導線を、利用者決定済みの仮問い合わせ先として確認。正式な非公開窓口と公開版での最終採用は未確認

## 2026-09-03 Chrome 152での名称更新後QA

- Haita表示名を`注ぐ！止める！タイミングチャレンジ / POUR & STOP: TIMING CHALLENGE`へ更新し、説明を現実の製品操作に見えにくいタイミングゲーム表現へ変更
- Node.js構文、66日英キー、6作品、参照欠落0、重複ID 0、外部リンク安全属性を再検査して合格
- Chrome `152.0.7977.65` Headlessで共通6画面幅と640×360・422×195を日英確認。横あふれ0、表示中44px未満操作対象0、意図しない文字切れ0
- 長い日英Haita名は390×844でもカード内に折り返され、説明と操作を覆わないことを補助目視
- 日記詳細の開閉、日英切替、再読込後のEnglish保持、主要Tab順と可視focus、390×844のprivacy日英本文を確認
- DOMContentLoaded約274ms、load約301ms、175 rAF / 3,014.9ms（約58.0/s）。同一PCのローカル事前証拠で、公開回線・物理実機の合格には数えない
- warning／error、HTTP 4xx/5xx、意図しない外部リクエストは0件。検査後にローカルサーバーを終了
- 対象9点の共通方式構造SHA-256は`8A7EED74CB51310A799A38A01FBC6C4B94C5F407241E7D815A2102A4BB2A0C3C`

## 公開版との差 2026-09-03

- 既存公開URL`https://okuyamatakaki.github.io/Portfolios/`はHTTP 200、390×844で横あふれ0
- 公開版には`日本語 / English`切替、ローカル候補の翻訳属性、プライバシー導線がなく、Haita表示も旧名`ハイターチャレンジ`のまま
- 公開版のChrome 152 HeadlessはWebGPUの`powerPreference`既知警告1件を記録。ローカル候補では同警告とWebGPU処理を検出していない
- ローカルHEADは`origin/main`より3commit先で、さらに今回の2ファイルが未commit。公開版が古いのはローカル修正失敗ではなく、push未実施による差
- 公開更新は外部送信を伴うため自動実行せず、送信差分、権利、戻し方、人の直前確認を揃える

## 作業開始時の安全確認

- Git worktreeは正本1件のみ
- Gitロックファイルなし
- 正本を対象とする別のGit、Node、Python、Unityライターなし
- ローカル確認サーバーの残存なし
- 既存の未コミット変更は保持

## 旧画像3点

次の3点は以前の作業でごみ箱へ移動済みで、現在の作業フォルダーにはありません。コミット開始時にGitで削除として識別され、この記録を含むローカルcommitの対象です。

- `assets/images/og-industrial.png`
- `assets/images/og.png`
- `assets/images/block-breaker.png`

現行の `index.html`、`privacy.html`、CSS、JavaScriptから3点への参照は0件です。本検証では削除・移動・復元を行っていません。必要時はごみ箱またはローカルHEAD `6cba0d5` から、対象を限定して復元します。

## 静的検査

環境: Node.js `v24.19.0`

```text
ローカライズ検査OK: 66キー / 日本語・English / 欠落0 / 未使用0
サイト静的検査OK: 6作品 / 参照欠落0 / 重複ID 0 / 安全属性OK
```

- `node --check assets/js/localization.js`: 合格
- `git diff --check`: 合格
- 秘密情報パターン: 0件
- 公開画面・スクリプト内のメールアドレス・携帯番号: 0件
- iframe、YouTube、Google Fonts、旧画像3点の実行時参照: 0件
- 素材台帳に記録した主要6ファイルのSHA-256: 現物と一致

## 実ブラウザ検証

実機ではなく、ローカルブラウザの表示領域を切り替えて確認しました。

| 表示 | 言語 | 横あふれ | 画面外要素 | 44px未満の操作対象 | iframe | 警告・エラー |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| 390×844 | 日本語 | 0 | 0 | 0 / 19件 | 0 | 0 |
| 390×844 | English | 0 | 0 | 0 / 19件 | 0 | 0 |
| 390×844 privacy | 日英併記 | 0 | 0 | 0 / 3件 | 0 | 0 |
| 1280×720 | 日本語 | 0 | 0 | 0 / 19件 | 0 | 0 |
| 1280×720 | English | 0 | 0 | 0 / 19件 | 0 | 0 |

追加確認:

- 言語ボタン、カテゴリ、技術タグなどの補助文字は共通の12.8px
- 日記アプリの「主な機能 / View key features」を開閉可能
- English選択は再読み込み後も保持
- プライバシーリンクから `privacy.html` へ移動可能
- 当時参照していた旧ルーレットPNGは549×661、SHIFT BREAKER画像は1280×720で読み込み完了。旧PNGは現在の候補では使用せず、新しいルーレットSVGへ差し替え済み
- 実行時に観測した資産は同一オリジンのCSS、JavaScript、PixelMplus、作品画像2点のみ。動画0件
- PC幅でレトロ工場風の配色、ピクセル文字、`06`表示を保ち、文字の重なりなし

## 外部リンク

2026-09-01に、現在のHTMLへ掲載した正確なURLをブラウザで読み取り確認しました。

- GitHubプロフィール: 到達、プロフィール名を確認。利用者決定により現在候補の仮問い合わせ先とする。正式な非公開窓口と公開版での最終採用は未確認
- Spring Boot日記アプリ: 正確なURLへ到達。無料ホスティングの起動待ち画面を確認したが、約25秒ではログイン画面の起動完了までは確認できず
- 日記アプリのGitHubリポジトリ: 到達、リポジトリ名を確認
- 2D タワーディフェンスゲーム: 到達、作品名を確認
- SHIFT BREAKER: 到達、作品名を確認
- ハイターチャレンジ: 到達、作品名を確認
- 30秒！草かりチャレンジ: 到達、公開先とサイト内の作品名を一致させた

ルーレットには公開URLを追加していません。確認できないURLを推測しない方針を維持します。

## 公開版との差

ネットワーク更新をせず、ローカルの `origin/main` を公開基準として比較しました。

- 公開基準: 日本語固定、プライバシーリンクなし、YouTube iframe 1件、旧 `og.png` と `block-breaker.png` を参照
- 現在の候補: 日本語 / English切替、日英プライバシー画面、iframe 0件、旧画像参照0件、第一者記録付きOG画像とSHIFT BREAKER画像を参照
- 現在の候補はローカルcommitとして保存し、未pushのため公開ページへは未反映

## 残件

- 新しいルーレットSVGとfaviconの制作記録・表示内容を人が確認する。AIの制作記録だけで公開承認にしない
- 日記アプリは起動完了後のログイン画面を再確認する
- Androidスマートフォン20台、iPhone 20台、AndroidタブレットまたはiPad 20台、Windows PC 20台の計80台を、公開リリースごとに実機確認する。現在0 / 80台
- 実Chrome／Safariの200%表示、30 FPS相当、20秒以内の操作開始、クラッシュ後復帰を計測する
- 独立した人が、目的の分かりやすさ、文字、操作、誤操作、復帰の快適性を確認する
- 公開前に、旧画像3点の削除を含む最新ローカルcommit全体を人が `git show` で確認する

## 後片付け

確認用ブラウザタブとローカルサーバーを終了し、表示領域の一時設定を解除します。本検証ではファイル削除、Git index変更、commit、push、公開、バックアップを行いません。
